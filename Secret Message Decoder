#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

int main() {
    string S;
    char C;
    
    cin >> S >> C;
    
    // std::count calculates occurrences directly
    cout << count(S.begin(), S.end(), C) << "\n";
    
    return 0;
}
